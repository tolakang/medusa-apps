# =====================================================================
# ecommerce / storefront — environment (customer storefront)
#
# Two kinds of values:
#   BUILD-TIME  -> Dokploy: Environment > Build-time Arguments
#                  (baked into the bundle; change = rebuild)
#   RUNTIME     -> Dokploy: Environment > Environment variables
# =====================================================================

# ---- BUILD-TIME ARGUMENTS ---------------------------------------------
# PUBLIC backend URL, e.g. https://api.example.com (used by the browser and the build; baked in)
NEXT_PUBLIC_MEDUSA_BACKEND_URL=
# Publishable API key (Admin > Settings > Publishable API Keys)
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
# This storefront's public URL, e.g. https://shop.example.com
NEXT_PUBLIC_BASE_URL=
# Fallback country code (ISO-2 lowercase)
NEXT_PUBLIC_DEFAULT_REGION=us
# Stripe publishable key (empty if not using Stripe)
NEXT_PUBLIC_STRIPE_KEY=
# Hostname of product images for next/image. Decision D3 = AWS S3, so this is
# <bucket>.s3.<region>.amazonaws.com (the host part of the backend's S3_FILE_URL)
S3_IMAGE_HOSTNAME=
# Allowed image path pattern
S3_IMAGE_PATHNAME=/**

# ---- RUNTIME VARIABLES --------------------------------------------------
NODE_ENV=production
PORT=8000
# Optional internal URL for server-side calls. Only read after starter change 6
# (BUILD_PLAN.md §4.2); the stock starter uses NEXT_PUBLIC_MEDUSA_BACKEND_URL only.
MEDUSA_BACKEND_URL=http://ecommerce-backend:9000
# Only if you add an on-demand revalidation route (the starter has none)
REVALIDATE_SECRET=CHANGE_ME
